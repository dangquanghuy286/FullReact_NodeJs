import type { Conversation } from "@/types/chat";
import React from "react";

const GroupMessageCard = ({ convo }: { convo: Conversation }) => {
  return <div>GroupMessageCard</div>;
};

export default GroupMessageCard;
